{# TODO Faire le style de la page #}
{% extends 'base.html.twig' %}

{% block title %}SteamID{% endblock %}

{% block content %}
    <h2 class="text-center">Vous pouvez ajouter ou modifier votre steam ID ici</h2>
    <hr>
    {% if notification %}
        <div class="alert alert-info text-center">{{ notification }}</div>
        <hr>
    {% endif %}
    <h3>Vous pouvez trouvez votre steamID en cliquant <a href="#">ici</a> !</h3>
    {{ form(form)  }}
    <br>
    <button type="button" class="btn btn-secondary">Revenir à mon compte !</button>
{% endblock %}

